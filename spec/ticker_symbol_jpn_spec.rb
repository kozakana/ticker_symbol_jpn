require 'spec_helper'

describe TickerSymbolJpn do
  it 'has a version number' do
    expect(TickerSymbolJpn::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
